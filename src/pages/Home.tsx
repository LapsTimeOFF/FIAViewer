import { Typography } from "@mui/material";
import Layout from "./Layout";

const Home = () => {
  return (
    <Layout>
      <Typography variant="h2" textAlign={"center"}>
        FIAViewer
      </Typography>
    </Layout>
  );
};

export default Home;
